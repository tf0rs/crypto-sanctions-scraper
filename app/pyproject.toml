[project]
name = "crypto-sanctions-scraper"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "sqlalchemy",
    "requests",
    "markdownify",
    "beautifulsoup4",
    "pypdf",
    "stamina",
]
